{
  "name": "gestor",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n VITE,API -c blue,red 'vite' 'json-server ./database/db.json -p 3000'",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "vite": "^7.0.3"
  },
  "dependencies": {
    "axios": "^1.10.0"
  }
}
